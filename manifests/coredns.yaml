---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: coredns
  namespace: skate
  labels:
    app: coredns
spec:
  selector:
    matchLabels:
      app: coredns
  template:
    metadata:
      labels:
        app: coredns
    spec:
      hostNetwork: true
      volumes:
      - name: cni
        hostPath:
          path: /var/lib/skatelet/cni/podman
      containers:
      - name: coredns
        image: ghcr.io/skateco/coredns
        volumeMounts:
        - mountPath: /var/lib/skatelet/cni/podman
          name: cni
        env:
        - name: CORE_FILE
          value: |
            cluster.skate:5553 {
            
                bind lo 0.0.0.0
            
                hosts /var/lib/skatelet/cni/podman/addnhosts
            }
            
            cluster.skate:53 {
            
                bind lo 0.0.0.0
            
                cache 3
            
                fanout . 127.0.0.1:5553 %%fanout_list%%
            
                loadbalance round_robin
            
            }
            .:53 {
                bind lo 0.0.0.0
                forward . 8.8.8.8
                cache 30
            }

